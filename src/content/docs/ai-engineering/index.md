---
title: AI Engineering
description: Math foundations, transformer internals, retrieval systems, fine-tuning and agents.
category: Math
tags: ["ai-engineering", "index"]
sidebar:
  order: 0
---

Five categories, ordered roughly by dependency.

1. **Math** — linear algebra, probability and optimisation, only as far as it is load-bearing.
2. **Transformers** — attention, positional encoding, KV caching, inference arithmetic.
3. **RAG** — chunking, embeddings, hybrid retrieval, reranking, evaluation.
4. **Fine-Tuning** — LoRA/QLoRA, dataset construction, eval harnesses.
5. **Agents** — tool use, planning loops, failure modes and guardrails.

[KV Cache Arithmetic](./transformers/kv-cache-arithmetic/) is the reference shape for a note
in this domain. The remaining categories are empty so far.
