---
title: Data Structures & Algorithms
description: Pattern-indexed problem notes with complexity analysis and parallel implementations.
pattern: Other
difficulty: Easy
timeComplexity: "n/a"
spaceComplexity: "n/a"
sidebar:
  order: 0
---

Problems are indexed by **pattern**, not by source or difficulty, because the pattern is
what transfers. Every note under `dsa/` declares its `pattern`, `difficulty` and both
complexity bounds; `leetcodeUrl` is optional.

Implementations are kept in Python, Go and C++ via the `<CodeTabs />` component so the
same algorithm can be compared across memory models.
